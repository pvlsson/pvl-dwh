models:
  - name: stg_updated_imdb_ratings
    description: Current IMDB data with correct field formatting
    columns:
      - name: imdb_id
        description: The unique key for each movie
        data_tests:
          - not_null
          - unique
